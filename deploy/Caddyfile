# Caddy слушает HTTPS на 8443 с самоподписанным сертификатом
# (его выпустит внутренний CA Caddy). Браузер один раз попросит «Принять риск».

:8443 {
    tls internal
    encode gzip
    reverse_proxy voice-assistant:8080
}
